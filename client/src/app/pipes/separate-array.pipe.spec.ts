import { SeparateArrayPipe } from './separate-array.pipe';

describe('SeparateArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new SeparateArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
